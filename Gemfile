source "https://rubygems.org"

gem 'github-pages', group: :jekyll_plugins

# For local development on Ruby 3.3+ where the github-pages gem's pinned
# nokogiri 1.15 fails to build, run with: BUNDLE_GEMFILE=Gemfile.local bundle exec jekyll serve
